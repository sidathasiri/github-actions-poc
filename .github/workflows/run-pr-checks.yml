name: pr-checks
run-name: ${{ github.actor }} is running pr checks
on: pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    environment: 
      name: dev
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Make changes
        run: |
          # make your changes
          echo "Random change: $RANDOM" >> random-change.txt
          branch_name="$(git rev-parse --abbrev-ref HEAD)-docs"
          git checkout -b "$branch_name"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@noreply.github.com"
          git add .
          git commit -m "Automated changes"
          git push origin "$branch_name"

      - name: Create PR
        run: |
          gh pr create --base main --head "$(git rev-parse --abbrev-ref HEAD)-docs" --title "Automated PR for documentation updates" --body "This PR was created by a workflow" --assignee "${{ github.actor }}"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}